language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "nightly"
  - "pypy"
# command to install dependencies
install:
 - pip install .
 - pip install -r requirements.txt
# command to run tests
script: make -C SimilarityCalculator/ test
# To specify environment variables, use the env key. Example:
# env: PYTHONPATH=/some_path/here